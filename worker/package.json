{
  "name": "worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "hono": "^4.6.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "latest"
  }
}
